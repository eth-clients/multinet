kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: common-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  selector:
    matchLabels:
      name: common-data